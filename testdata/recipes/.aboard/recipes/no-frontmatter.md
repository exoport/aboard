# This file has no frontmatter at all

Which makes it invalid. It must still be LISTED, marked invalid, rather than
silently skipped — a file the author is looking at that the tool pretends does
not exist is the worst of the three outcomes.
