---
name: future-schema
description: "Requires a board schema this binary does not write."
when_to_use: "Never; it exists so the min_schema marker has something to mark."
requires: { min_schema: 99 }
---

# future-schema

Marked, not hidden: a reader can still open it and see what they are missing.
