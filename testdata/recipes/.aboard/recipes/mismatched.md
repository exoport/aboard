---
name: something-else
description: "The frontmatter name does not match the file stem."
when_to_use: "Never; it exists so the stem check has something to catch."
---

# mismatched

`aboard recipes show` takes the name, so a name that does not match the stem is
a recipe nobody can open by looking at the directory.
