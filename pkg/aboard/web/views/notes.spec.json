{
  "type": "notes",
  "label": "Notes",
  "blurb": "Free text. The escape hatch for anything the other types do not fit.",
  "since": 1,
  "init": {
    "text": ""
  },
  "state": {
    "text": {
      "type": "string",
      "doc": "the note"
    },
    "markdown": {
      "type": "boolean",
      "doc": "render it, with a Read/Edit toggle"
    }
  },
  "controls": [
    {
      "id": "reload",
      "label": "Reload text",
      "doc": "Re-read the note after an external change."
    },
    {
      "id": "copy",
      "label": "Copy all",
      "doc": "Copy the whole note."
    },
    {
      "id": "mode",
      "label": "Edit",
      "title": "Edit the markdown source",
      "doc": "Toggle between the rendered note and its source. Only when `markdown` is on."
    }
  ],
  "gestures": [
    "type freely",
    "Tab indents (Escape then Tab to leave)"
  ],
  "keys": [],
  "notes": [
    "The markdown subset: headings, lists, code, quotes, rules, links, emphasis.",
    "A ```mermaid fence renders as a diagram, through the same loader and the same board-token theme the `diagram` tab uses \u2014 so a figure can sit inside a write-up instead of needing a tab of its own. A fence mermaid cannot parse shows its source verbatim.",
    "Only when `markdown` is on: with it off the note is plain text and a fence is just three backticks."
  ]
}
