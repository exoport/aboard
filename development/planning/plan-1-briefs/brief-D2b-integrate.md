# Phase D2b brief — integrate the staged tooling, skill, docs, handoffs, fixture notes

Repo `/home/diegos/_dev/exoport/aboard`, HEAD = the rename commit. Governing plan:
`development/planning/plan-1_port-from-spike.md`. Staging root:
`S=/tmp/claude-1000/-home-diegos--dev-ai-board/0e329b78-9ff1-4b58-bd23-4efd89232c39/scratchpad/staging/`.

**You own**: everything EXCEPT `pkg/`, `cmd/`, `go.mod`, `go.sum`, `testdata/recipes/`. Another
agent is concurrently implementing Go features in those (root `--name`, `aboard init`, `aboard
recipes list|show|index`, `aboard gen-docs --out`, `aboard version` with exported
`Version`/`BuildDate`/`GitCommit`, bb359/bb360). Do not touch its files; do not commit; do not
`git add`. Do not run `make caps`, `make docs-cli`, `make lint`, `make test` yet — the Go side is
moving under you; the next phase runs them. Never touch the spike (`/home/diegos/_dev/ai/board`).

Read each staging area's `NOTES.md` first: `$S/tooling/NOTES.md`, `$S/skill/NOTES.md`,
`$S/docs/NOTES.md`, `$S/fixture/NOTES.md`, `$S/recipes/INDEX-FORMAT.md`, and the handoff agent's
output in `$S/handoffs/` and `$S/vscode/`.

## 1. Tooling → repo root

Copy `$S/tooling/*` (everything except `NOTES.md`) into the repo root: `.bingo/`, `Makefile`,
`.golangci.yaml`, `.goreleaser.yaml`, `.github/workflows/{ci,release}.yml`, `.gitignore`,
`.gitattributes`, `.pre-commit-config.yaml`, `.markdownlint.yaml`, `.markdownlintignore`,
`.prettierrc.yaml`, `.prettierignore`, `LICENSE`, `NOTICE`, `CHANGELOG.md`, `scripts/`. The
current `Makefile` (from the split) has targets the staged one must keep working: `caps` (build,
emit `pkg/aboard/web/views/controls.generated.js` and `.claude/skills/aboard/references/reference.generated.md`
and `references/recipes.md`, build AGAIN, `aboard capabilities --check`), `smoke`, `shot`, `dev`,
`run`, `status`, `build`, `check` — diff the two and merge so nothing is lost; use `<file>.tmp && mv`
for generated outputs as the staged one does. `.gitignore` must contain `/aboard`, `/.aboard/`,
`/dist/`, `/.shots/`, `go.work`, `go.work.sum`, and `_output/*` except `_output/.gitignore`
(create `_output/.gitignore` as ape has it). Carry the mermaid provenance file the NOTICE points at:
check `pkg/aboard/web/lib/README.md` exists (the spike had `vendor/README.md`); if it is missing,
copy it from `/home/diegos/_dev/ai/board/vendor/README.md` (read-only source) — that ONE file under
`pkg/` is yours to add because it is not Go and the other agent will not touch it.
Run `make -n build`, `make -n caps`, `make -n ci-local`, `make help` and fix what does not parse.
Run `make tools` (bingo installs the pinned tools into `.bingo`-managed paths; network is available)
and report what it installed.

## 2. Skill → `.claude/skills/aboard/`

The rename commit `git mv`'d the spike's skill to `.claude/skills/aboard/` with its OLD text.
Replace the authored files with `$S/skill/*` (SKILL.md and `references/*` except the two generated
ones). Keep the committed `references/reference.generated.md` as is (the next phase regenerates it);
create `references/recipes.md` as the placeholder the staging left (regenerated by `make caps`).
Delete authored files from the old skill that the staging did not carry ONLY if the staging
`NOTES.md` says they were merged/dropped deliberately; otherwise keep and rename-fix them. Also copy
ape's two skills: `/home/diegos/_dev/exoport/apex_process_ape/.claude/skills/handoff/` verbatim
into `.claude/skills/handoff/`, and `.claude/skills/release/` with the ape-only gates DELETED (the
`make check-prices` / `make check-harness` phases — read the file; remove those phases entirely and
renumber, do not leave them as "skip"). Copy `.claude/.gitignore` and `.claude/.markdownlint.yaml`
from ape.

## 3. Docs → repo root and `docs/`

Copy `$S/docs/CLAUDE.md` → `CLAUDE.md`, `$S/docs/README.md` → `README.md`, `$S/docs/docs/` →
`docs/`. Then reconcile against reality: `pkg/aboard/commands.go` at HEAD is the command surface
(plus the commands the other agent is adding per the plan: `init [--example] [--gitignore]`,
`recipes list|show [--template]`, root `--name`, hidden `recipes index` and `gen-docs`). Every
command in CLAUDE.md, README.md and docs/ must be one of those — grep for `aboard ` in the docs and
check each. `docs/reference/cli.md` stays a placeholder (generated later). Run
`python3 scripts/docs-check.py` (or whatever the staged Makefile's `docs-check` runs) and fix
every broken link / unreachable page.

## 4. Handoffs → `development/handoffs/` and the VS Code repo

Copy `$S/handoffs/*.md` → `development/handoffs/`. Add `development/README.md` (one paragraph:
planning/ = plans, handoffs/ = briefs for a later session, in ape's style — read
`/home/diegos/_dev/exoport/apex_process_ape/development/` for the shape). Update
`development/planning/plan-1_port-from-spike.md`'s "Status" line to say the three commits landed
(hashes from `git log`) and that Phase D is in progress.
VS Code repo: `/home/diegos/_dev/exoport/aboard_vscode` is empty. `git init -b main` there, copy
`$S/vscode/README.md` and `$S/vscode/docs/handoff.md`, add a `.gitignore` (node_modules/, out/,
dist/, *.vsix, .aboard/), and commit ONE commit there (`-c user.name="Diego Szychowski"
-c user.email="diegopodo@gmail.com"`, message in the repo style, trailer
`Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`). That repo is yours to commit; the aboard
repo is NOT (the orchestrator commits it).

## 5. Fixture

The example board is embedded by the other agent at `pkg/aboard/example/aboard.json`; do NOT create
`testdata/example-board/`. Read `$S/fixture/NOTES.md` and make sure `docs/` and CLAUDE.md describe
`aboard init --example` consistently with what it contains (15 tabs, which examples).

## 6. Consistency sweep (read-only on the other agent's files)

`grep -rn 'board\.json\|\.board/\|\./board\|board -' CLAUDE.md README.md docs/ .claude/ development/`
→ zero hits that mean the old world (the phrase "the board" as a noun is fine). `grep -rn
'window.board\|__BOARD_DATA__\|__board\b\|\bboard\.\(get\|set\|fit\|onData\)(' .claude/ docs/
README.md CLAUDE.md development/` → zero hits.

## Report

Files copied/created/deleted (with counts per area); every `make` invocation and its result; `make
tools` output; docs-check result; the aboard_vscode commit hash; every command string that appears in
the docs/skill (deduplicated) so the next phase can execute each one; deviations; undone.

## 7. Also

- Reconcile the staged Makefile's help text `.shots/` with `layout.go`'s `.aboard/run/shots/`
  (the code wins; `test/shot.sh` already writes there — verify).
- Add to `test/smoke.sh` a four-line bridge-name assertion: the served `/tab/bb72/html` defines
  `window.aboard` and `__ABOARD_DATA__` and contains zero occurrences of `window.board`,
  `__BOARD_DATA__`, `__board`. (You own `test/`.)
- `docs/reference/http-api.md` documents absent `__by` → `unknown`; the Go agent is implementing
  exactly that now, so leave it. Add one line to `docs/explanation/why-two-identities.md` (or the
  layout reference) that a pre-rename `.board/` is not recognised, by design.
- The skill's NOTES.md and the docs' NOTES.md list every command they contain: concatenate them
  (deduplicated) into `$S/doc-commands.txt` for the next phase to execute.
