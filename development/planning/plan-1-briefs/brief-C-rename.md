# Phase C brief — the rename (commit 3 of 3)

Repo: `/home/diegos/_dev/exoport/aboard`, HEAD = the split (pkg/aboard + cli + cmd/board, cobra).
Governing plan: `development/planning/plan-1_port-from-spike.md` — settled. This commit changes
NAMES only. No structural change, no new feature. Everything that is called `board` because it
names the product becomes `aboard`; everything that is `board` because it is a DATA CONTRACT
stays. The line between those two is the whole job — record every judgement in the commit body.

## Renames (product name → `aboard`)

| what | from | to |
|---|---|---|
| binary and cmd dir | `cmd/board`, `-o board` | `cmd/aboard`, `-o aboard` |
| `AppName` and hosts | `"board"`, `"ape-board"` | `"aboard"`, `"ape-aboard"` |
| runtime dir | `.board/` | `.aboard/` (content: `aboard.json`, `uploads/`, `recipes/`; runtime: `run/…`) |
| state file | `board.json`, `board.<name>.json` | `aboard.json`, `aboard.<name>.json` |
| atomic temp prefix | `.board-*.json` | `.aboard-*.json` |
| the state route | `GET/POST /board.json` | `GET/POST /aboard.json` (browser fetches, smoke.sh, apply client, everything) |
| env | `BOARD_NAME` | `ABOARD_NAME` |
| shell file | `board.html` | `aboard.html` (embed line, serve path, reload signature) |
| page `<title>`, header text, help-panel text, notices, `document.title`, the "board running at" CLI lines, log prefixes, cobra `Use`/`Short`/`Long` | "board" | "aboard" — read each in context: "the board" as a noun for the thing on screen ("put it on the board", "board is read-only") stays English; the product/command name changes |
| localStorage keys | inspect `board.html` and `views/*.js` for every key (`board.*`, `board:` …) | `aboard.*` — list them |
| html-tab bridge — **exact inventory, verified against the spike's `htmltab.go` `bridgeScript` and `views/html.js`** (7 symbols): `window.__BOARD_DATA__` (injected `<script>` + read by the bridge); `window.board` (the bridge object) with `.get()`, `.set(next)`, `.onData(fn)`, `.fit()`; and the envelope **PROPERTY** `__board` — it is NOT a prefix: frames post `{__board:'set', data}` and `{__board:'height', height}`, the parent posts `{__board:'data', data}`, and `views/html.js` matches `msg.__board === 'set' / 'height'`. Widgets call the bridge as a BARE global (`board.get()`, `board.set(…)`, `board.fit()`), not `window.board.get()` — grep the CALL FORMS (`\bboard\.(get|set|onData|fit)\(`), not just `window.board`. Also the hint text in `views/html.js` ("It persists state by calling board.set(value); read it back with board.get()"). | `window.__ABOARD_DATA__`, `window.aboard` + `aboard.get/set/onData/fit`, envelope property `__aboard` — **no alias, no fallback** (DECIDED). Sites: `htmltab.go` (injects both the data global and the bridge script, matches `e.data.__board`), `views/html.js`, `views/stack.js` (ctxForBlock), the smoke suite's widget probes, `pkg/aboard/web/test/*.html`, and the seeded state copy used for the ladder |
| skill dir | `.claude/skills/board/` | `.claude/skills/aboard/` — `git mv`; SKILL.md `name: aboard`; the generated-reference path constant in caps.go AND the Makefile target (both — a mismatch makes `--check` print "nothing to check" and pass forever) |
| `--by` default and doc | `agent-1` stays; any text saying "board" as the product | — |
| Makefile `BINARY`, restart.sh, test/smoke.sh, test/shot.sh, `.gitignore` | `board` | `aboard` (restart.sh reads `.aboard/run/instance…json`) |
| `.gitignore` | `/board`, `/.board/`, `/board.*.json` | `/aboard`, `/.aboard/` |
| caps manifest `app` | `"board"` | `"aboard"` — capsHash moves; regenerate |
| generated files | `controls.generated.js`, `reference.generated.md` | regenerated by `make caps` (build twice) |
| `README.md`, `CLAUDE.md` | leave the PROSE alone (they are rewritten in a later phase) — but fix any COMMAND they show so it runs (`./board -x` → `aboard x`), because a doc command is a claim | |

## Stays (data contract — do NOT rename; say so in code comments where a reader would wonder)

- the `bb` id tag (ids.go, board.html id allocator)
- the state document's schema keys (`tabs`, `state`, `stateFrom`, `nextId`, `version`, `lastEditedBy`, …) and the `board` Go type name if you judge it clearer as the document type — either way, be consistent
- `by: "claude"` matching in `views/chat.js` (it reads history)
- the `bb133` gallery's deliberate unknown `sparkline` component
- the `views/*.spec.json` `type` values (`dag`, `kanban`, …) and `TYPES`/`MODULES` registries
- HTTP route names other than `/board.json` (`/health`, `/capabilities`, `/tab/<id>/html`, `/events`, `/wait`, `/poke`, `/waiters`, `/journal`, `/watch`, `/log`, `/upload`, `/uploads/…`)
- `mermaid` classDef / `--agent` token etc. — untouched

## Method

1. `grep -rniw 'board' --include='*.go' --include='*.js' --include='*.html' --include='*.css' --include='*.json' --include='*.sh' --include='Makefile' --include='*.md' . | grep -v '^./_output' | grep -v 'lib/mermaid'` → save the list to `/tmp/…/scratchpad/rename-before.txt`. Work through it file by file; no blind `sed -i` across the tree (it would hit `bb` ids in prose, "the board" as a noun, and the mermaid bundle).
2. `git mv` for file/dir renames so history follows.
3. After the code: `make caps` (twice-built), `aboard capabilities --check` → 0.
4. Ladder (same as Phase B): build/vet/gofumpt/test; seed `.aboard/aboard.json` from the spike's board.json BUT with the bridge names rewritten in the copy (a python one-off on the copy, applied ONLY inside html-tab / html-block `html` strings: `window.board`→`window.aboard`, the call forms `board.get(`/`board.set(`/`board.onData(`/`board.fit(`→`aboard.…(`, `__BOARD_DATA__`→`__ABOARD_DATA__`, the property `__board`→`__aboard`; a ready-made converted board also exists at `/tmp/claude-1000/-home-diegos--dev-ai-board/0e329b78-9ff1-4b58-bd23-4efd89232c39/scratchpad/staging/fixture/testdata/example-board/aboard.json` — you may seed from that instead, and if you do, LOOK at every example tab, since nobody has rendered it yet) — the spike's file itself is NEVER modified and its server on 46624 is never touched; start detached; smoke ONCE (timeout 180000); shots of gallery, `/tab/bb72/html`, `#help` — LOOK at them: the sketch pad must still draw and its `aboard.set` must persist (check `state.data` changes after a simulated stroke, or at least that the frame loads with no console error in `chromium --dump-dom`); stop by pid; delete the binary.
5. Final grep: `grep -rniw 'board' …` again → `rename-after.txt`; every survivor must be one of the "Stays" or a legitimate English noun. Put the survivor categories and counts in the commit body.
6. Commit once, repo message style, `-c user.name="Diego Szychowski" -c user.email="diegopodo@gmail.com"`, trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. Do not push.

## Report back

Commit hash; old and new `capsHash`; the rename table as actually applied (including every
localStorage key and bridge symbol); the survivor categories with counts; ladder numbers; the
PNGs you looked at and what they showed; deviations.
